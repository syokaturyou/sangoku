<div class="container">
  <h2>ジャンル名編集</h2>
   <div class="row">
    <div class="col-lg-11">
      <%= form_with model:@genre,url: manager_genre_path, local:true do |f| %>
        <div style = "display:flex">
         <%= f.text_field :name %>
         <%= f.submit '変更を保存', class: 'btn btn-success' %>
        </div>
      <% end %>
   </div>
  </div>
</div>